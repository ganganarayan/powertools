# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*~

# Node (only if tooling is ever added)
node_modules/
npm-debug.log*
yarn-error.log*

# Environment / secrets
.env
.env.*

# Claude local machine settings (keep out of shared repo)
.claude/settings.local.json
